import { AngularCLIPipe } from './angular-cli.pipe';

describe('AngularCLIPipe', () => {
  it('create an instance', () => {
    const pipe = new AngularCLIPipe();
    expect(pipe).toBeTruthy();
  });
});
